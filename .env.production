# Production PostHog configuration, read by Vite at build time (`vite build`).
# The project token is a public, client-side write key (it is embedded in the
# browser bundle by design), so it is safe to commit here.
VITE_POSTHOG_PROJECT_TOKEN=phc_wFfAFVtXAs5MnyxgZZEZSaDq8igAvNx2DC8S34eezSo4
VITE_POSTHOG_HOST=https://us.i.posthog.com
